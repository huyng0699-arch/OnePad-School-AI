import { messages } from "../../lib/demoData";
export default function Page(){return <><section className="hero"><div className="hero-top"><div><div className="kicker">Messages</div><h2>Parent and internal communication center.</h2><p>Draft parent-safe messages from teacher-visible summaries.</p></div></div></section><section className="section split"><div className="card solid"><h3>Inbox</h3>{messages.map((m)=><div className="message-card" key={m.id}><div><strong>{m.subject}</strong><p>{m.from} · {m.student} · {m.last}</p></div><span className="badge amber">{m.status}</span></div>)}</div><div className="card solid"><h3>Compose Safe Reply</h3><label>Recipient<select>{messages.map((m)=><option key={m.id}>{m.from} - {m.student}</option>)}</select></label><label>Template<select><option>Learning progress update</option><option>Review plan</option><option>Attendance follow-up</option></select></label><textarea rows={7} defaultValue="Hi, here is a safe learning-focused update: we are using a short review plan and will check progress after the next practice task." /><div className="pill-row"><button>Save draft</button><button>Send message</button><button>Ask AI to make warmer</button></div></div></section></>}

