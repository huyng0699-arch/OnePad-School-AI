"use client";
import { useState } from "react";
import { class8AStudents } from "../../lib/demoData";
export default function Page(){ const [mode,setMode]=useState("Balanced"); const groups=["A - Lab captains","B - Diagram builders","C - Explain-back","D - Review crew"]; return <><section className="hero"><div className="hero-top"><div><div className="kicker">Group Work</div><h2>Form groups with learning signals and clear roles.</h2><p>Each group gets a task, collaboration role, and teacher check-in action.</p></div><div className="status-pill"><span className="dot" /> {mode} mode</div></div></section><section className="section card solid"><div className="toolbar"><select value={mode} onChange={(e)=>setMode(e.target.value)}><option>Balanced</option><option>Support-first</option><option>Stretch challenge</option></select><button>Regenerate groups</button><button>Assign collaboration rubric</button></div></section><section className="section grid cols-4">{groups.map((g,idx)=><div className="card solid" key={g}><h3>{g}</h3><p>{idx===0?"Model cell roles":idx===1?"Draw transport diagram":idx===2?"Teach-back checkpoint":"Focused review"}</p>{class8AStudents.filter((s)=>s.group===g).map((s)=><div className="mini-person" key={s.id}>{s.fullName}<span>{s.biologyMastery}%</span></div>)}<button>Open group board</button></div>)}</section></> }

